name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        registry: registry.alexbissessur.dev
        username: ${{ vars.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        
    - uses: actions/checkout@v4
    
    - name: Get Docker version
      run: echo "APP_VERSION=$(cat version.txt)" >> $GITHUB_ENV
      
    - name: Output Docker ver
      run: echo $APP_VERSION

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and push
      uses: docker/build-push-action@v6
      with:
        platforms: linux/amd64,linux/arm64
        push: true
        tags: registry.alexbissessur.dev/meetups:${{ env.APP_VERSION }}

    # - name: Build and push Docker image
    #   run: docker buildx build --platform linux/amd64,linux/arm64 . -t registry.alexbissessur.dev/meetups:$APP_VERSION --push

    # - name: Build the Docker image
    #   run: docker build . --file Dockerfile --tag registry.alexbissessur.dev/meetups:$APP_VERSION

    # - name: Push Docker image
    #   run: docker push registry.alexbissessur.dev/meetups:$APP_VERSION
